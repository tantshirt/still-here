# Audio provenance

Scene audio uses three CC0-processed WAV beds generated in the audio spike (rig hum loop, placement click, dull drop one-shots). Files live under `public/audio/` and are loaded at runtime after the visitor unlock gesture.
